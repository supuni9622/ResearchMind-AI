"""
Memory Platform (memory_platform_prd.md).

Owns storage, retrieval, and lifecycle of memory across conversations,
research sessions, and future agent executions. Independent from the
Retrieval Platform, Research Runtime, Agent Runtime, and Workspace
Runtime -- it provides persistent intelligence those platforms consume,
and performs no reasoning, planning, or workflow execution of its own
(PRD §3 Non-Goals).
"""
