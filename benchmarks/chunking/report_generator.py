from benchmarks.common.report_generator import BenchmarkReportGenerator


class ChunkingBenchmarkReportGenerator(
    BenchmarkReportGenerator,
):
    """
    Generates reports for the Chunking Benchmark.

    Currently inherits the generic benchmark report generator.
    Future versions may add chunking-specific visualizations and
    statistics.
    """

    pass
