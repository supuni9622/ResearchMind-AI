from benchmarks.common.report_generator import BenchmarkReportGenerator


class EmbeddingBenchmarkReportGenerator(
    BenchmarkReportGenerator,
):
    """
    Generates reports for the Embedding Benchmark.

    Currently inherits the generic benchmark report generator.
    Future versions may add embedding-specific visualizations and
    statistics.
    """

    pass
